# Finley — BrokerAgent dependencies
# Install: pip install -r requirements.txt
# After install, run: playwright install chromium

playwright==1.44.*
supabase==2.5.*
pydantic==2.7.*
httpx==0.27.*
python-dotenv==1.0.*
